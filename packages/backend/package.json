{
  "name": "@swflcoders/backend",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "./build-lambda-binaries-zig.sh",
    "build:pipeline": "./build-lambda-binaries-zig.sh",
    "build:lambda": "./build-lambda-binaries-zig.sh",
    "build:lambda:clean": "./build-lambda-binaries-zig.sh --clean",
    "build:lambda:docker": "./build-lambda-binaries-docker.sh",
    "build:lambda:docker:clean": "./build-lambda-binaries-docker.sh --clean",
    "build:lambda:native": "./build-lambda-binaries.sh",
    "build:lambda:smart": "./build-lambda-smart.sh",
    "build:lambda:zig": "./build-lambda-binaries-zig.sh",
    "build:native": "cargo build --release",
    "dev": "cargo run",
    "test": "cargo test",
    "clean": "cargo clean",
    "type-check": "cargo check"
  },
  "dependencies": {
    "@swflcoders/types": "workspace:*"
  }
}
