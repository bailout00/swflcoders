{
  "name": "@swflcoders/cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy:beta": "cdk deploy --context stage=beta",
    "deploy:gamma": "cdk deploy --context stage=gamma", 
    "deploy:prod": "cdk deploy --context stage=prod",
    "diff": "cdk diff",
    "synth": "cdk synth",
    "clean": "rm -rf dist/ cdk.out/ node_modules/",
    "type-check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^22.0.0",
    "jest": "^29.4.0",
    "ts-jest": "^29.1.0",
    "aws-cdk": "2.87.0",
    "typescript": "~5.0.4"
  },
  "dependencies": {
    "aws-cdk-lib": "2.87.0",
    "constructs": "^10.0.0",
    "@swflcoders/types": "workspace:*"
  }
}
